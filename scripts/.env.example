# Data Verification Environment Variables
# Copy to .env and fill in your actual values

# Option 1: Use a single connection string (RECOMMENDED for Supabase)
PG_DSN="postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-0-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require"

# Option 2: Use discrete connection parameters (alternative)
# PGHOST=aws-0-us-east-1.pooler.supabase.com
# PGPORT=5432
# PGDATABASE=postgres
# PGUSER=postgres.YOUR_PROJECT_REF
# PGPASSWORD=YOUR_PASSWORD
# PGSSLMODE=require

# Optional: Customize output directory
# OUTPUT_DIR="reports/datavalidation"

# Optional: Customize verification time windows
# ACTIVE_DAYS=7
# HIST_YEARS=3

# Optional: Customize staleness thresholds (minutes)
# STALENESS_WARNING_MINUTES=5
# STALENESS_CRITICAL_MINUTES=15

# Optional: Override table names
# REGISTRY_TABLE=core_asset_registry_all
# DATA_BARS_TABLE=data_bars
# DERIVED_BARS_TABLE=derived_data_bars
